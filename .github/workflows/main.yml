name: KnowledgeGraph CI/CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # -----------------------------
      # PYTHON ENVIRONMENT
      # -----------------------------
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Python tests
        run: |
          if [ -d tests ]; then pytest -q; else echo "No Python tests found"; fi

      # -----------------------------
      # NODE + HARDHAT ENVIRONMENT (Optional)
      # -----------------------------
      - name: Set up Node.js
        if: hashFiles('package.json') != ''
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Node dependencies
        if: hashFiles('package.json') != ''
        run: |
          npm install

      - name: Compile Solidity contracts
        if: hashFiles('package.json') != '' && hashFiles('hardhat.config.ts') != ''
        run: |
          npx hardhat compile

      # -----------------------------
      # OPTIONAL: DEPLOY TO SOMNIA
      # -----------------------------
      - name: Deploy KGProvenance to Somnia Testnet
        if: github.ref == 'refs/heads/main' && hashFiles('scripts/deploy_kg_provenance.ts') != ''
        env:
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
        run: |
          npx hardhat run scripts/deploy_kg_provenance.ts --network somnia

      # -----------------------------
      # OPTIONAL: LINTING
      # -----------------------------
      - name: Python linting
        run: |
          pip install flake8
          flake8 agents || true

      - name: Node linting
        if: hashFiles('package.json') != ''
        run: |
          npm install --save-dev eslint
          npx eslint . || true
